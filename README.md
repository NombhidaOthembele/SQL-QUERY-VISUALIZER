# SQL-QUERY-VISUALIZER
Paste any SQL SELECT query and get a colour-coded visual breakdown by clause (SELECT, FROM, JOIN, WHERE, GROUP BY, etc.) with complexity scoring and query stats. Includes 4 built-in sample queries.
